# EduVid Factory — агентная система для создания обучающих видео с ReAct-циклом

## Что за задача и для кого?
Вы создаёте образовательные короткие видео. Хотите автоматизировать процесс, но при этом получать точный, хорошо структурированный контент, который не выглядит шаблонным. Обычные пайплайны просто генерируют текст по шаблону — это быстро, но может быть поверхностно.

**EduVid Factory** — это **агентная система**, которая:
- самостоятельно планирует, какую информацию нужно найти,
- выполняет поиск в интернете,
- анализирует найденное,
- пишет диалог,
- при необходимости уточняет детали через повторный поиск,
- только после этого запускает синтез речи и сборку видео.

Агент действует как сценарист: он рассуждает, проверяет себя и улучшает результат.

## Что делает PoC (демо-версия)?
PoC реализует базовый **ReAct-агент**:
1. Принимает тему (например, «градиентный бустинг»).
2. **Планирует**: агент формулирует, какие именно аспекты темы нужно раскрыть в диалоге.
3. **Действует (инструмент)**: вызывает поисковый API (Google Custom Search / SerpAPI) для получения определений и фактов.
4. **Наблюдает**: получает результаты поиска.
5. **Рассуждает**: анализирует, достаточно ли информации. Если нет — уточняет запрос и ищет снова (до 3 итераций).
6. **Генерирует финальный диалог** в формате JSON.
7. Далее стандартный конвейер: TTS → видео.

Агент имеет краткосрочную память (история запросов и результатов) в рамках одного сеанса.

## Что НЕ делает PoC
- Не имеет долговременной памяти между запусками.
- Не использует другие инструменты (только поиск).
- Не обрабатывает мультимодальные запросы (только текст).
- Не публикует видео автоматически в соцсети (только сохраняет на диск).
- Не собирает статистику просмотров и лайков.
- Не имеет веб-интерфейса — только командная строка.
- Не поддерживает потоковую генерацию или очередь заданий.
- Не обрабатывает авторские права на голоса знаменитостей (используются обобщённые синтезированные голоса).
